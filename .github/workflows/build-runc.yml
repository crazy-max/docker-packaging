name: build-runc

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      pkgs:
        description: 'Packages to build (minimal, all or space separated list)'
        default: 'minimal'
        required: false
        type: string
  push:
    branches:
      - 'main'
      - 'runc/[0-9]+.[0-9]+'
    paths:
      - '.github/workflows/.build.yml'
      - '.github/workflows/build-runc.yml'
      - 'common/**'
      - 'pkg/runc/**'
  pull_request:
    paths:
      - '.github/workflows/.build.yml'
      - '.github/workflows/build-runc.yml'
      - 'common/**'
      - 'pkg/runc/**'

jobs:
  run:
    uses: ./.github/workflows/.build.yml
    with:
      name: runc
    secrets: inherit
